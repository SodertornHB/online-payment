@* --------------------------------------------------------------------*@
@* Warning! This is an auto generated file. Changes may be overwritten *@
@* --------------------------------------------------------------------*@
@using Localization
@using Microsoft.Extensions.Options
@inject LocService SharedLocalizer
@model OnlinePayment.Web.ViewModel.PaymentResponseViewModel

@{
    ViewData["Title"] = $"{SharedLocalizer.GetLocalizedHtmlString("Delete")} {SharedLocalizer.GetLocalizedHtmlString("PaymentResponse")}";
}

<h1>@($"{SharedLocalizer.GetLocalizedHtmlString("Delete")} {@SharedLocalizer.GetLocalizedHtmlString("PaymentResponse")}")</h1>

<h3>@SharedLocalizer.GetLocalizedHtmlString("Are you sure you want to delete this?")</h3>
<div>
    <hr />
    <dl class="row">

        <dt class = "col-sm-2">
            <label asp-for="Session" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Session")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Session
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="Location" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Location")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Location
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="PaymentReference" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("PaymentReference")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.PaymentReference
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="Status" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Status")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Status
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="PaymentResponseReceivedDateTime" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("PaymentResponseReceivedDateTime")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.PaymentResponseReceivedDateTime
        </dd>
    </dl>
    <form method="post"  enctype="multipart/form-data" asp-controller="PaymentResponse" asp-action="Remove">    
        @Html.HiddenFor(x => x.Id)
        <input type="submit" value="@SharedLocalizer.GetLocalizedHtmlString("Delete")" class="btn btn-danger" /> |
        <a asp-controller="PaymentResponse" asp-action="Index">@SharedLocalizer.GetLocalizedHtmlString("Back to List")</a>
    </form>
    </div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

