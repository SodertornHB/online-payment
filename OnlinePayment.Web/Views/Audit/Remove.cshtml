@* --------------------------------------------------------------------*@
@* Warning! This is an auto generated file. Changes may be overwritten *@
@* --------------------------------------------------------------------*@
@using Localization
@using Microsoft.Extensions.Options
@inject LocService SharedLocalizer
@model OnlinePayment.Web.ViewModel.AuditViewModel

@{
    ViewData["Title"] = $"{SharedLocalizer.GetLocalizedHtmlString("Delete")} {SharedLocalizer.GetLocalizedHtmlString("Audit")}";
}

<h1>@($"{SharedLocalizer.GetLocalizedHtmlString("Delete")} {@SharedLocalizer.GetLocalizedHtmlString("Audit")}")</h1>

<h3>@SharedLocalizer.GetLocalizedHtmlString("Are you sure you want to delete this?")</h3>
<div>
    <hr />
    <dl class="row">

        <dt class = "col-sm-2">
            <label asp-for="Session" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Session")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Session
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="Message" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Message")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Message
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="DateTime" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("DateTime")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.DateTime
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="Entity" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("Entity")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Entity
        </dd>
    </dl>
    <form method="post"  enctype="multipart/form-data" asp-controller="Audit" asp-action="Remove">    
        @Html.HiddenFor(x => x.Id)
        <input type="submit" value="@SharedLocalizer.GetLocalizedHtmlString("Delete")" class="btn btn-danger" /> |
        <a asp-controller="Audit" asp-action="Index">@SharedLocalizer.GetLocalizedHtmlString("Back to List")</a>
    </form>
    </div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

