@* --------------------------------------------------------------------*@
@* Warning! This is an auto generated file. Changes may be overwritten *@
@* --------------------------------------------------------------------*@
@using Localization
@using Microsoft.Extensions.Options
@inject LocService SharedLocalizer
@model OnlinePayment.Web.ViewModel.MigrationViewModel

@{
    ViewData["Title"] = $"{SharedLocalizer.GetLocalizedHtmlString("Delete")} {SharedLocalizer.GetLocalizedHtmlString("Migration")}";
}

<h1>@($"{SharedLocalizer.GetLocalizedHtmlString("Delete")} {@SharedLocalizer.GetLocalizedHtmlString("Migration")}")</h1>

<h3>@SharedLocalizer.GetLocalizedHtmlString("Are you sure you want to delete this?")</h3>
<div>
    <hr />
    <dl class="row">

        <dt class = "col-sm-2">
            <label asp-for="ClientVersion" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("ClientVersion")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.ClientVersion
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="DatabaseVersion" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("DatabaseVersion")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.DatabaseVersion
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="CreatedOn" class="control-label">@SharedLocalizer.GetLocalizedHtmlString("CreatedOn")</label>
        </dt>
        <dd class = "col-sm-10">
            @Model.CreatedOn
        </dd>
    </dl>
    <form method="post"  enctype="multipart/form-data" asp-controller="Migration" asp-action="Remove">    
        @Html.HiddenFor(x => x.Id)
        <input type="submit" value="@SharedLocalizer.GetLocalizedHtmlString("Delete")" class="btn btn-danger" /> |
        <a asp-controller="Migration" asp-action="Index">@SharedLocalizer.GetLocalizedHtmlString("Back to List")</a>
    </form>
    </div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

